Step 1 — Check the root user authentication method

1.In MySQL Workbench, open a new SQL tab.

2.Run this query:

in powershell
SELECT user, host, plugin FROM mysql.user WHERE user='root';

Step 2 — If plugin is caching_sha2_password

Run this SQL to fix it:

in powershell
ALTER USER 'root'@'localhost'
IDENTIFIED WITH mysql_native_password
BY 'Harrisun_758';
FLUSH PRIVILEGES;

Step 3 — Restart backend
in powershell
node server.js

You should see:

Server listening on port 8081
Database connected successfully

Email verification in SQL
ALTER TABLE users
  ADD COLUMN verified TINYINT(1) NOT NULL DEFAULT 0,
  ADD COLUMN verify_token VARCHAR(255) NULL,
  ADD COLUMN reset_token VARCHAR(255) NULL,
  ADD COLUMN token_expiry DATETIME NULL;
 
password_hash VARCHAR(255) 
INSERT INTO users (username, password_hash, email) VALUES (?, ?, ?)
